# Host port. It is bound to 127.0.0.1 for the reverse proxy only.
APP_PORT=3000

# Runtime PostgreSQL connection. This user needs only the application-level database privileges.
# Add sslmode=require when the database requires TLS.
DATABASE_URL="postgresql://user:password@db.example.internal:5432/base?schema=pdf_loader"

# PostgreSQL connection for Prisma migrations. Use a separate user with permissions to modify the
# pdf_loader schema. The database host, database, and schema must match DATABASE_URL.
MIGRATION_DATABASE_URL="postgresql://migration_user:password@db.example.internal:5432/base?schema=pdf_loader"

# Optional: separate connection/schema for the external edition source.
EXTERNAL_EDITION_DATABASE_URL="postgresql://user:password@db.example.internal:5432/base"
EXTERNAL_EDITION_SCHEMA="idz_ukr"
EXTERNAL_EDITION_SOURCE_CODE="idz-ukr"
EXTERNAL_EDITION_SOURCE_NAME="IDZ-UKR"

# Authentication
AUTH_SECRET="replace-with-a-long-random-secret"
AUTH_TRUST_HOST="true"

# Used only by `pnpm prisma:seed`; seeding is not performed by Docker startup.
ADMIN_LOGIN="admin"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="ChangeMe123!"
ADMIN_NAME="Admin"
